spring:
  # DB 연결

# 기존에 링크 연결 시 생략하며 연결해주는 역할, but 이것땜에 타임리프 안 되는 상황, 주석처리하면 타임리프 링크 연결 가능
#  mvc:
#    static-path-pattern: /static/**

  datasource:
    # 설치된 DB와 연결 URL
    url: jdbc:log4jdbc:oracle:thin:@localhost:1521:xe
    # 접속을 위한 드라이버
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    #oracle.jdbc.driver.OracleDriver
    username: danaka
    password: danaka

server:
  port: 8090



